header:
  - titles:
      # @start locale config
      en      : &EN       HTB
    url: /HTB.html
    
  - titles:
      # @start locale config
      en      : &EN       THM
    url: /THM.html

  - titles:
      # @start locale config
      en      : &EN       CTFs
      # @end locale config
    url: /ctfs.html

  - titles:
      # @start locale config
      en      : &EN       Archive
    url: /archive.html
   
  - titles:
      # @start locale config
      en      : &EN       Daily_Update
      # @end locale config
    url: /daily_update.html

  - titles:
      # @start locale config
      en      : &EN       About
    url: /about.html
