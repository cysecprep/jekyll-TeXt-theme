header:
  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /home.html
    
  - titles:
      # @start locale config
      en      : &EN       HTB
    url: /HTB.html
    
  - titles:
      # @start locale config
      en      : &EN       THM
    url: /THM.html

  - titles:
      # @start locale config
      en      : &EN       CTFs
      # @end locale config
    url: /ctfs.html

  - titles:
      # @start locale config
      en      : &EN       About
    url: /about.html
